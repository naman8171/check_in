<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_stripe_fee_form_view" model="ir.ui.view">
        <field name="name">payment.transaction.stripe.fee.form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form" />
        <field name="arch" type="xml">

            <xpath expr="//field[@name='amount']" position="after">
                <field
                    name="stripe_fee_amount"
                    string="Stripe Extra Fee"
                    readonly="1"
                    modifiers="{'invisible': [
                        '|',
                        ['provider_code', '!=', 'stripe'],
                        ['stripe_fee_amount', '=', 0.0]
                    ]}" />
                <field
                    name="stripe_fee_is_international"
                    string="International Fee"
                    readonly="1"
                    widget="boolean"
                    modifiers="{'invisible': [
                        '|',
                        ['provider_code', '!=', 'stripe'],
                        ['stripe_fee_amount', '=', 0.0]
                    ]}" />
            </xpath>

        </field>
    </record>

    <record id="payment_transaction_stripe_fee_list_view" model="ir.ui.view">
        <field name="name">payment.transaction.stripe.fee.list</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount']" position="after">
                <field
                    name="stripe_fee_amount"
                    string="Stripe Fee"
                    optional="show"
                    modifiers="{'invisible': [['provider_code', '!=', 'stripe']]}" />
            </xpath>
        </field>
    </record>

</odoo>